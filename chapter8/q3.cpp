#include<iostream>
#include<cstring>
#include<cctype>

using namespace std;
bool Toupper(string &);

int main() {
    string name;
    cout<<"Please Enter a string(q to quit): ";
    getline(cin,name);
    while(name[0])
    
}

